# SocialAutoBot

> A social media scheduling and automation tool for multiple platforms

## ✨ Features
- User authentication with JWT
- CRUD operations for main resources
- RESTful API with proper status codes
- Database migrations with Alembic
- Docker containerization

## 🏗️ Architecture
REST API backend with Python, frontend SPA, and relational database

## 🚀 Quick Start

### Prerequisites
- Python 3.11+
- Docker & Docker Compose
- PostgreSQL (or use Docker)

### Setup

```bash
# Clone the repository
git clone https://github.com/elitsuri/socialautobot
cd socialautobot

# Copy environment variables
cp .env.example .env
# Edit .env with your configuration

# Using Docker (recommended)
cd docker
docker-compose up -d

# Or run locally
cd backend
pip install -r requirements.txt
uvicorn main:app --reload
```

### API Documentation
Once running, visit:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## 📁 Project Structure

```
socialautobot/
├── backend/          # FastAPI backend
│   ├── main.py       # App entry point & routes
│   ├── models.py     # SQLAlchemy ORM models
│   ├── schemas.py    # Pydantic schemas
│   ├── database.py   # DB connection
│   └── auth.py       # JWT authentication
├── frontend/         # Web frontend
│   ├── index.html
│   ├── app.js
│   └── styles.css
├── database/         # SQL scripts
├── docker/           # Docker configuration
└── README.md
```

## 🔑 Environment Variables

| Variable | Description | Default |
|----------|-------------|---------|
| `DATABASE_URL` | PostgreSQL connection string | sqlite:///./app.db |
| `SECRET_KEY` | JWT signing secret | (required) |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | Token expiry | 30 |

## 📄 License

MIT License — feel free to use and modify.

---
*Generated by AI Project Factory — [github.com/elitsuri](https://github.com/elitsuri)*
